<div id="rooms" class="container w3-section w3-animate-opacity">
	<p>{{@error}}</p>
	<p class="w3-btn-floating w3-blue w3-center"><a href="{{@room}}" >+</a></p>
	<!--
		This is where the original table will displayed. The user then has the option 
		of loading more profile pages(That is, the user can load more items here )
	-->
	<repeat group="{{@rooms}}" value="{{@room}}">
		<div id={{@room['id']}} class="w3-row w3-card-4 w3-margin-8 w3-white">
			<div class="w3-quarter w3-border w3-center">
				<img src="content/{{@image}}" style="width:100%;" alt="image"></img>
			</div>
			<div class="w3-twothird w3-padding-8 w3-center">
				<p class="w3-quarter">{{@room['username']}}</p>
				<p class="w3-quarter">{{@room['firstname']}}</p>
				<p class="w3-quarter">{{@room['lastname']}}</p>
			</div>
		</div>
	</repeat>
	<div class="w3-right">
		<button class="w3-btn w3-blue w3-margin-8">load more</button> 
	</div>
</div>
<script>
$(function(){
	
});
</script>
